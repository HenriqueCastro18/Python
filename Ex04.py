#1. Vc quer simular a opção de jogar uma moeda e resultar em cara ou coroa
    #Jogue as opções em uma variável e depois crie um programa que imprime se é 'cara' ou 'coroa' na tela

import random

lado_moeda = ('cara','coroa')

print(random.choice(lado_moeda))
